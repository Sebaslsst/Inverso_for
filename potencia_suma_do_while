#include <stdio.h>

int potencia(int base, int exponente) {
    int resultado = 1;
    int i = 0;
    do {
        resultado *= base;
        i++;
    } while(i < exponente);
    return resultado;
}

int main() {
    int base = 8;
    int exponente = 3;
    int resultado = potencia(base, exponente);
    printf("%d ^ %d = %d\n", base, exponente, resultado);
    return 0;
}
